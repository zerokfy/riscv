#include    "declare.inc"

module ram_wrap {
    OnChipRAM   ram;

    {
        ram.clock   = m_clock;
        ram.address = addr[8:0];
        ram.data    = wdata;
        ram.rden    = rden;
        ram.wren    = wren;
        rdata       = ram.q;
    }
}

